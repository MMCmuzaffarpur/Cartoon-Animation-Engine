# Third-party notices

Phase 1 introduces provisional dependencies only: Node.js (MIT), TypeScript (Apache-2.0), Zod (MIT), Fastify (MIT), better-sqlite3 (MIT), SQLite (public domain), Vitest (MIT), ESLint (MIT), tsx (MIT). Exact resolved versions, transitive dependencies, hashes and notices must be captured from the lockfile before release. No media, AI, renderer, FFmpeg, Blender, model, or paid-service dependency is included.
