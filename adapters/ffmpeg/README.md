# FFmpeg Adapter Boundary

The core does not bundle a proprietary encoder. `packages/video-engine` invokes a user-selected local FFmpeg executable through an explicit path/configuration. Pin and review the exact FFmpeg build/license before redistribution.
