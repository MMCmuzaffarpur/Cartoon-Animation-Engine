# Bounded Technical Evaluations

Each evaluation is a plan, not an approval or dependency selection. Candidates are compared against functionality, license, installation, CPU/GPU/memory requirements, determinism, packaging, maintenance and integration complexity. Record results, exact versions/build flags and unresolved issues in a follow-up ADR proposal when an architectural decision must change.
