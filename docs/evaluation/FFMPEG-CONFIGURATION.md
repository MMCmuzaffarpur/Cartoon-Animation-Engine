# Evaluation: FFmpeg configuration

Evaluate approved-build candidates for decode/encode/mux, image sequences, audio filters, subtitle support, codec availability, LGPL/GPL configuration, notices, static/dynamic distribution obligations, deterministic encoder settings, CPU/memory and packaging. No build configuration is approved.
