# Rig

`RigDefinition`: rig type; skeleton bones with parent/rest transforms/semantic roles; animator controls; constraints with solve order; 2D deformers or 3D skin/morph data; facial mapping; hand mapping; retarget profile; capabilities; compatibility. Solver behavior is defined by an algorithm/version reference.
