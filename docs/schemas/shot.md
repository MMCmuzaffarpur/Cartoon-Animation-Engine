# Shot

`Shot`: scene/sequence reference; active camera; tick range; framing/movement tracks; transition references; render intent; capability requirements; camera fallback policy. A shot does not own renderer settings beyond portable intent.
