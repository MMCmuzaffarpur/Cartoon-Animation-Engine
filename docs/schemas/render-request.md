# Render Request

`RenderRequest`: pinned project/sequence revision; tick range; preset/custom settings; resolution/frame rate/color intent; image/video/audio output requirements; backend preference; cache policy; priority; required/preferred capabilities; idempotency key.
