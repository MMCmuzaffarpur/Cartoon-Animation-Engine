# Character Variant

`CharacterVariant`: ID; base Character Assembly revision; sparse override delta; reason/name/tags; compatible contexts; optional seed override; provenance. Resolution applies the delta to the pinned base in stable order.
