# Motion Layer

`MotionLayer`: layer ID/type; source clip or procedure; target mask/control set; blend mode; weight curve; tick range; priority; seed; algorithm ID/version; serialized parameters; required rig capabilities; bounds; fallback/baked output reference. Evaluation order is base, masks, gestures, additive, facial, gaze, IK, procedural, limits.
