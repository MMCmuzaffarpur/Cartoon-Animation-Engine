# Project

`ProjectManifest`: identity; schema/version settings; tick rate/frame rate/units/color intent; asset-library references; Character Definitions/Assemblies; scenes; sequences; render presets; revision graph; extension config; compatibility history. A portable export pins every included asset revision and license record.
