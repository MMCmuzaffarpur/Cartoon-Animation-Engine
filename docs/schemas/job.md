# Job

`Job`: ID/type/state/priority; immutable request payload; idempotency key; worker capability requirements; stage progress; cancellation; retry policy/attempts; checkpoints; logs/errors; input/output artifacts; timestamps. States are queued, preparing, running, finalizing, succeeded, cancelling, cancelled, failed, retrying.
