# Asset Variant

`AssetVariantRecipe`: base asset revision; variant ID; deterministic transformation/template parameters; representation/LOD/style constraints; derived asset reference when materialized; compatibility; provenance. A variant never copies or replaces the base source asset.
