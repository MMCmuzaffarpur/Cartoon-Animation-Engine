# Render Manifest

`RenderManifest`: render request; resolved project/assets; evaluated-state hashes/ranges; selected backend/version/capabilities; environment fingerprint; output artifact hashes; cache provenance; reproducibility level; warnings/fallbacks; job log references.
