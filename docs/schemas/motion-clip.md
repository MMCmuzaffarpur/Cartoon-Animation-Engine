# Motion Clip

`MotionClip`: duration ticks; loop mode; target tracks; interpolation/keyframes; events; root motion; semantic category; source/retarget profile; rig requirements. Target identity uses semantic bone/control/property paths, not renderer objects.
