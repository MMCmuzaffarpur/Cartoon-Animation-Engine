# Error Codes

Stable error envelope: `{ error: { code, message, details } }`. Codes are defined in `packages/domain/src/index.ts`, including validation, revision, asset, command, capability, entity, plan and job failures.
