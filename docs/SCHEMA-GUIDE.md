# Schema Guide

Persisted documents are JSON-compatible, schema-versioned, canonicalized before hashing, and retain only namespaced extensions. Unknown required semantics are rejected by Zod contracts. Timing uses integer ticks and rational frame rates.
