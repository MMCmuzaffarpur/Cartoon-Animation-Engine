# Development

Runtime target: Node 24.x and npm 11.x. Commands: `npm install`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run schema:validate`. Local data defaults to `.local-data`; never commit it.
